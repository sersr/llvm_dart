

fn main() i32 {
  let y = return_value();

  printf("value: %d\n", y.x);
  printf("value: y %d\n", y.y);
  0;
}

struct Base {
  x: i32,
  y: i32,
}

fn return_value() Base {

  let x = Base {10, 20 };
  printf("inner: %d\n", x.y);
  x
}