
import "std:d.kc";

fn main() i32 {
  let option = Some(10, 11);

  match option {
    Some(x: i32, y: i32) => {
      printf("x: %d, y: %d\n", x, y);
    }
  }

  0;
}

struct Box {
  x: i32,
}

enum Option<T,S> {
  Some(T,S),
  None(),
}