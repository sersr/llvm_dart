
fn main() i32 {
  final v = Vec<i32>.new();
  v.add(10);

  printf("add 10\n");
  printf("v.len %d\n", v.getLength());
  let x = v.pop();

  match x {
    Some(a) => {
      printf("pop x: %d\n", a);
    },
    None => {
      printf("......\n");
    }
  }

  printf("v.len %d\n", v.getLength());

  0;
}
