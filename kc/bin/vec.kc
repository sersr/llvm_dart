import "std:allocator.kc"

struct Vec<T> {
  data: *T,
  size: usize,
}


impl Vec<T> {
  static fn new(size: usize) Vec<T> {
    final data = malloc(sizeOf(T) * size);
    Vec { data, usize }
  }

  fn drop() {
    free(self.data);
    self.size = 0;
  }
}


fn main() i32 {

  0;
}