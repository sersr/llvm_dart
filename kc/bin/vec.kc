import "std:allocator.kc"
struct Vec<T> {
  data: *T,
  size: *usize,
}
impl<T> Vec<T> {
  static fn new(size: usize) Vec<T> {
    final data = malloc(sizeOf(T) * size);
    final sizePtr = malloc(sizeOf(usize)) as *usize;
    *sizePtr = size;
    Vec { data, sizePtr }
  }
}