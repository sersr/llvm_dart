extern fn printf(str: string, ...) i32;

fn print(str: string) {
    printf("log: %s\n", str);
}

struct Array<T> {
    size: usize,
    pointer: cArray<T>,
}

type cArray<T>;

impl Array<T> {

    static fn new(size: usize) T {

    }
    
    fn getSize() usize {
        return self.size;
    }

    fn elementAt(index: usize) &T {

    }
}

impl Array<i8> {
  fn toStr() cArray<i8> {
    self.pointer
  }
}