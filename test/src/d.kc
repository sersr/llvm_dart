extern fn printf(str: *u8, ...) i32;

fn print(str: *u8) {
    printf("log: %s\n", str);
}

struct Array<T>;

impl Array<T> {

    static fn new(size: usize) T;
    
    fn getSize() usize;

    fn elementAt(index: usize) &T;
}

impl Array<u8> {
  fn toStr() &string;
}

type Ptr<T>;
struct CArray<T> {
  size: usize,
  ptr: **T,
}

impl CArray<T> {
  fn elementAt(index: usize) &T;
  fn getSize() usize {
    self.size
  }
}