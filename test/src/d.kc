extern fn printf(str: string, ...) i32;

fn print(str: string) {
    printf("log: %s\n", str);
}

struct Array<T> {
    size: usize,
}

type cArray<T>;

impl Array<T> {
    static fn fromC(size: usize,argv: cArray<T>) Array<T>{

    }
    fn getSize() usize {
        return self.size;
    }

    fn elementAt(index: usize) T {

    }
}