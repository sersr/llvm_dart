import 'd.kc';

fn main() i32 {
    let g = Gen { 3 };
    g.pp();
    let arr:[Gen; 5] = <Gen>[];
    let arr2 = Array<Gen>{11,2,3,23,3, size : 55};
}

struct Gen {
    y: i32,
}

impl Gen {
    fn pp() {
        let y = 444;
        fn inx() {
            printf("y: %d\n", y);
        }
        inx();
    }
}