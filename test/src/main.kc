import 'd.kc';


struct Data {
	x: i32,
	y: i64,
}

struct Bcx<T> {
	x: i32,
	b: T,
}

impl Bcx<T> {
	fn getb(n: Gen) &T {
		let y = &self.b;
		self.b = n;
		let h = n;
		printf("h: %d\n", h.x);
		y;
	}
}

struct Gen {
	x: i32,
}

impl Gen {
	fn printx() {
		printf("gen.x: %d\n", self.x);
	}
}

fn main() i32 {
	print('start');

	let bcx = Bcx {34343 ,Gen { 3434343 }}
	let size = bcx.getb( Gen { 66666 });
	printf("size: %d\n", size.x);
	0;
}